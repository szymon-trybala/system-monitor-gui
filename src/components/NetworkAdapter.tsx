import * as React from 'react';

export interface NetworkAdapterProps {
    
}
 
export interface NetworkAdapterState {
    
}
 
class NetworkAdapter extends React.Component<NetworkAdapterProps, NetworkAdapterState> {
    state = {   }
    render() { 
        return ( <div><p>Adapter sieciowy</p></div> );
    }
}
 
export default NetworkAdapter;